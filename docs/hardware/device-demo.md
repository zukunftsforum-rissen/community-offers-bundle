Der Demo dient ausschließlich zu Demonstrations- und
Testzwecken.

Er verwendet nicht den produktiven Poll-Endpunkt
/api/device/poll und erzeugt daher keine Einträge in:

- tl_co_device.lastSeen
- tl_co_door_log

Der Device Monitor zeigt ausschließlich echte Geräte.